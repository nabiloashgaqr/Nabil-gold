# وثيقة تسليم الإصدار النهائي الشامل — BUILD 31 REVISION 9
**تاريخ الإصدار:** 2026-08-25  
**حزمة التوزيع المعتمدة:** `Nabil-gold-FULL-PROJECT-BUILD31-R9-20260825.zip`  
**الحالة التشغيلية:** 100% تم التحقق منها على سيرفر VPS المباشر (2,568 اختباراً ناجحاً في pytest).

---

## 1. ملخص التحديثات الهندسية الكبرى المنجزة

### أ. تكامل أزواج العملات الأربعة مع لاحقة البروكر `.s`
تمت إضافة أزواج العملات الأربعة إلى جانب الذهب والنفط مع الربط المباشر برموز شركة الوساطة:
* **الذهب:** `XAU/USD` $\rightarrow$ `XAUUSD.s` (وضع حي كامل `LIVE`)
* **النفط:** `WTI/USD` $\rightarrow$ `WTI.s` (وضع حي كامل `LIVE`)
* **اليورو/دولار:** `EUR/USD` $\rightarrow$ `EURUSD.s` (وضع مراقبة واختبار ذكي `Shadow`)
* **الباوند/دولار:** `GBP/USD` $\rightarrow$ `GBPUSD.s` (وضع مراقبة واختبار ذكي `Shadow`)
* **الدولار/كندي:** `USD/CAD` $\rightarrow$ `USDCAD.s` (وضع مراقبة واختبار ذكي `Shadow`)
* **الدولار/ين:** `USD/JPY` $\rightarrow$ `USDJPY.s` (وضع مراقبة واختبار ذكي `Shadow`)

### ب. تطبيق نفس القوانين الهندسية بدقة (Mathematical & Architectural Parity)
تم تطبيق نفس قوانين الذهب والنفط عبر معايرة معامل النقاط ($k$) وحجم النقطة (`point_size`) لكل زوج:
* **نطاق الوقف الآمن (Stop Loss):** حساب الوقف بالاعتماد على كتل السيولة الأبعد مع توفير هوامش أمان محسوبة لكل زوج.
* **الأهداف السعرية:** حجز الأرباح الجزئية عند الهدف الأول TP1 ($R:R \ge 0.8$)، والهدف الثاني TP2 ($2.0 \times R$).
* **نقل الوقف للدخول (Early Breakeven):** تفعيل كسر التعادل آلياً بمجرد تقدم الصفقة.
* **الوقف المتحرك (Trailing Stop):** تحريك الوقف لحماية الأرباح المتراكمة بخطوات رقمية دقيقة.
* **مسارات التنفيذ الستة (Paths 1 to 6):** توافق كامل مع جميع مسارات التحليل بما فيها منطقة الدخول الذهبية OTE (Path 6).

### ج. محرك فحص الأسعار الذكي والديناميكي (Dynamic Price Sanity Engine)
تم حل مشكلة النطاقات الثابتة بإدخال دالة `price_sanity_range` التي تقرأ الحدود الواقعية لكل أصل:
* الذهب: `[1500 - 6000]`
* النفط: `[20 - 200]`
* أزواج الفوركس القياسية: `[0.4 - 3.5]`
* أزواج الين الياباني: `[50 - 300]`

### د. سلسلة نماذج Gemini الذكية المزدوجة (Dual-Quota Fallback Chain)
تم ضبط السلسلة لتعمل بحصتين متكاملتين (500 + 500 = 1000 طلب يومياً):
1. **النموذج الأساسي:** `gemini-3.1-flash-lite` (500 RPD)
2. **الاحتياطي الأول:** `gemini-3.5-flash-lite` (500 RPD)
3. **الاحتياطي الثاني:** `gemini-3.6-flash`
4. **الاحتياطي الثالث:** `gemini-3.5-flash`

### هـ. مختبر التوثيق والقياس الإحصائي المستقل (Shadow Mode Lab)
تم إنشاء هيكل تخزين منفصل بالكامل تحت `storage/shadow/` لكل زوج من الأزواج الأربعة:
* حفظ خرائط اليوم (`session_plans.json`)
* توثيق تصويتات الوكلاء ونقاط القوة (`decision_audit.json`)
* تتبع الصفقات المحاكية والأرباح والوقف والأهداف (`trades.json`)
* قياس أداء المسارات 1 إلى 6 (`path_performance/`)
* سكربت تقرير الترقية الإحصائي `scripts/shadow_report.py` للمقارنة مع الذهب.

### و. ضبط إشعارات التيليجرام الساعية (Hourly Status & Dedup Guards)
* إرسال بطاقة الحالة الدورية (Market Status) تلقائياً عند رأس كل ساعة (`:01`) في النافذة الأولى (`minute < 10`).
* حجب الرسائل المكررة تلقائياً أثناء باقي دقائق الساعة لحماية القناة من الإزعاج.
* إرسال إشارات الصفقات الحقيقية والتحديثات الفورية لحظياً دون أي تأخير.

---

## 2. هيكل الحزمة البرمجية الكاملة

```
Nabil-gold-FULL-PROJECT-BUILD31-R9-20260825.zip
├── config.json                     <-- الإعدادات الشاملة للأزواج الستة وسلسلة Gemini ونظام Shadow
├── run_analysis.py / scripts/      <-- محرك التحليل والتنفيذ ومسارات الدخول 1-6
├── services/
│   ├── market_data.py              <-- جلب البيانات المتعددة مع التوافق مع لاحقة .s
│   ├── mt5_feed.py                 <-- اتصال MT5 اللحظي وتفعيل symbol_select
│   ├── shadow_mode.py              <-- قاعدة بيانات ونظام حماية Shadow المعزول
│   ├── telegram_bot.py             <-- إشعارات التيليجرام وبطاقات الحالة
│   ├── execution_paths.py          <-- مسارات الدخول الستة المعتمدة
│   └── llm_review.py               <-- الربط الذكي مع Gemini وسلسلة الفولباك
├── utils/
│   ├── instruments.py              <-- تعريفات النقاط والتحويلات وفحص الأسعار
│   └── helpers.py                  <-- قراءة الإعدادات وتهيئة البيئة
├── storage/                        <-- قواعد البيانات والقياسات المعزولة
│   ├── agent_measurements/
│   └── shadow/
│       ├── eurusd/
│       ├── gbpusd/
│       ├── usdcad/
│       └── usdjpy/
└── tests/                          <-- 2,568 اختباراً مؤتمتاً تغطي كافة أجزاء المنظومة
```

---

## 3. أوامر الفحص والتشغيل على السيرفر (Cheat-Sheet)

### 1. فحص اتصال MT5 والأسعار المباشرة للأزواج الستة:
```powershell
python scripts\verify_all_instruments_live.py
```

### 2. تشغيل دورة تحليل تجريبية شاملة:
```powershell
python scripts\run_analysis.py
```

### 3. عرض لوحة الأداء الإحصائي لأزواج Shadow ومقارنتها بالذهب:
```powershell
python scripts\shadow_report.py
```

### 4. إرسال تقرير الأداء الإحصائي مباشرة إلى التيليجرام:
```powershell
python scripts\shadow_report.py --send
```

---

## 4. حالة مهام Windows Task Scheduler على السيرفر

| اسم المهمة (Task Name) | الحالة (State) | الدور التشغيلي |
| :--- | :---: | :--- |
| **SS_DemoAnalysis** | `Running / Ready` | تنفيذ دورة التحليل كل 5 دقائق لجميع الأزواج وإرسال الحالة عند رأس الساعة |
| **NabilTickManager** | `Ready` | متابعة التكات الحية وإدارة الوقف المتحرك والأهداف والأوامر المعلقة كل 10 ثوانٍ |
| **NabilDashboard** | `Running` | لوحة التحكم المحلية المباشرة |

---
**المنظومة تم تسليمها وتحديثها بالكامل وهي في قمة الاستقرار والجاهزية التشغيلية.**
