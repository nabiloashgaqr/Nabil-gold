Updated files package for Nabil-gold.
Includes P0/P1 fixes, Supabase legacy schema hotfix, One-Agent + Groq decision mode,
and GitHub Actions test fix so DatabaseService local fallback is allowed during pytest.
Copy the Nabil-gold folder contents over your repository root, then run: python -m pytest -q
