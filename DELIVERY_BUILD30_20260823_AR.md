# ملف تسليم — Nabil-gold (نظام التداول الآلي Multi-Agent)
## نسخة: BUILD 30 — PRE-ARM OTE (التجهيز المسبق عند الارتداد المتوقع) — تاريخ: 2026-08-23

> **قرارك المطبَّق** (2026-08-22: «طبق كامل ويكون حي وقابل للقياس لا ظل»): وضع **التجهيز المسبق (Pre-Arming A)** يعمل **حياً** — الأمر يُرصد في لحظة **الاندفاع (impulse)** ويُحجز كطلب **LIMIT معلق** عند **الارتداد المتوقع OTE** *قبل* وصول السعر إليه — نفس الأبواب، صفر استدعاءات LLM جديدة، وبصمة قياس مستقلة (`armed_at`) في الصفقات والتقارير. **لا مرحلة ظل إطلاقاً.**
>
> **الرَقعة ذاتية الاكتمال** (27 ملفاً + manifest): كل ما سبق (BUILD 27 + إصلاح الاستيراد + BUILD 28 + مخزن جيميني + BUILD 29) + وضع التجهيز المسبق. تُطبَّق من أي نقطة سابقة بنفس الشكل.

---

## 1) الحزمة

| الملف | الوصف |
|---|---|
| `Nabil-gold-BUILD30-PREARM-OTE-20260823.zip` | 27 ملفاً (payload/ + manifest.json) تُطبَّق بـ `APPLY_PATCH.ps1 -Force` + دمج كتلة `pre_arm_ote` في config (إضافة فقط). |

**الاختبارات (بيئة العمل):** `2507 passed / 3 skipped / 0 failed` — منها **19 تثبيتاً جديداً** لوضع التجهيز. **على الـVPS المتوقع: 2507 passed / 3 skipped / 0 failed** (بيانات التسميات موجودة عنده أصلاً، ولديّ كانت تنقصها فاستُعيدت من المستودع — ليست جزءاً من الحزمة).

---

## 2) ما الذي بُني بالضبط

### الفكرة
النظام يدخل **بعد** اكتمال الارتداد (السعر يعود للـOTE ثم يستقر) — أي أن الدخول يتأخر في **السعر** (يدخل بعد ما فات من الارتداد) وفي **الوقت** (يقرر متأخراً). التجهيز المسبق يعكس ذلك: **يقرر في لحظة الاندفاع** ويحجز الطلب عند **الارتداد المتوقع**، فينفذ (fill) *أثناء* الارتداد لا بعده.

### الآلية (كلها حساب محلي — صفر LLM)
1. **رصد الاندفاع** (شموع 15m، آخر 15 شمعة، الشمعة الساقطة مستبعدة): رجل اندفاع قياسي — حجم ≥ **1.2×ATR(14)** وقمتها (أو قاعها) ضمن **آخر 3 شموع مغلقة**.
2. **بوابة الاتجاه الصارمة**: اتجاه SMC (خريطة الإطار الرئيسي) = `BULLISH` → تجهيز **BUY** فقط · `BEARISH` → **SELL** فقط · أي شيء آخر (RANGING/NEUTRAL) → **لا تجهيز**. Auction flow = بوابة ناعمة (تمنع فقط عند تحيز صريح معاكس).
3. **الأبواب الأخرى نفسها**: أخبار (block كامل)، صفقة موجودة على الرمز (OPEN/PARTIAL/TP1_HIT/PENDING) تمنع، سقف المسافة `risk_settings.max_pending_distance_points` (قيمة 0 = الحالي).
4. **هندسة OTE** (محسوبة محلياً من رجل الاندفاع):
   - **الدخول = 70%** من الارتداد · **المنطقة 62–79%** (تُثبَّت في الطلب للمراقبة).
   - **الستوب = نقطة بداية الرجل ∓ 1.0×ATR** · **الهدف = قمة/قاع الرجل**.
   - إن كان R:R عند القمة أقل من **1.5** → الهدف يمتد (تمديد الرجل) حتى 1.5R بدل رفض التجهيز.
   - **الـOTE يجب أن يكون على الجانب البعيد من السعر** (السعر فوق منطقة OTE للشراء = الارتداد لم يعد — لا تجهيز).
5. **التنفيذ الحي**: القرار يُحفظ كصفقة `PENDING` بنوع أمر `BUY_LIMIT/SELL_LIMIT` → **مدير النبض (tick manager) يضعه على وسيط MT5 كتطلب LIMIT حقيقي** بنفس مسار أي طلب معلق — عند اللمس ينفذ بالسعر الفعلي ويعمل عليه كل إدارة الصفقة (TP1/TP2/ستوب/ترييلينغ/خروج thesis) كما هي.
6. **تفاعل مع مسارات الدخول**: التجهيز **يُخمد الدخول التفاعلي في نفس الاتجاه لنفس الدورة** (عائلة طلب واحدة لكل رمز/اتجاه). الاتجاه المعاكس أو WAIT يسيران عاديان.

### ليس مساراً سادساً — وضع تجهيز
المسارات الخمسة تبقى مسارات **القبول (admission)** الوحيدة للتداول التفاعلي. التجهيز المسبق وضع **تجهيز** مستقل عليه **تسمية شفافة** في كل البطاقات والتقارير:
`🛣️ PRE-ARM OTE — PROJECTED RETRACEMENT (BUILD 30)` — تظهر كصف خاصة في «Execution Paths» الأسبوعي وفي أداء المسارات، منفصلة عن المسارات الخمسة (لا تلوث إحصاءاتها).

### قواعد الإلغاء (المطلوبة)
| القاعدة | التنفيذ |
|---|---|
| **MSS معاكس** (انهيار الهيكل ضد الاتجاه) | يعمل أصلاً على كل الطلبات المعلقة (حارس الهيكل) — لم يلمس ✓ |
| **اختراق كامل لمنطقة OTE بدون تنفيذ** | **قاعدة جديدة** `OTE_ZONE_CONSUMED`: السعر عبر المنطقة كاملة (الحافة البعيدة) دون لمسة نظيفة → الإلغاء بوصفه **مستهلكاً** |
| **الأخبار** | تعمل كما هي ✓ |
| **24 ساعة** | تعمل كما هي ✓ |

### قاعدة «المستهلك» (مضاد المطاردة)
تجهيز أُبطل (اختراق المنطقة / MSS) = **النفذ (setup) استُهلك** — لا يعيد النظام تجهيز مستوى قديم. إعادة القبول التفاعلي ممكنة فقط على **هيكل جديد** (اندفاع جديد)، لا بمطاردة مستوى فات.

### القياس («قابل للقياس»)
- حقل **`armed_at`** في صف الصفقة: `pre_arm_ote` أو فارغ (تفاعلي = السلوك التاريخي).
- قسم **Execution Quality** في التقرير الأسبوعي يضيف سطراً:
  `• Pre-arm OTE: N closed (✅ w · ❌ l · ➖ b, net ±X pts) | reactive M (net ±Y pts)`
- صف مستقل في **Execution Paths** + أداء المسارات (`path_performance`).
- هذا ما يقيس تأثير «الوقت/السعر» للدخول أسبوعاً بأسبوع — إن لم يبرر الأرقام التوسيع لمسارات التصويت (1/2/3/5) لاحقاً، يبقى على خريطة SMC + Auction كما هو.

### ما لم يلمس
محرك SL/TP، الترييلينغ، خروج الـthesis، حجب الأخبار، المقاسات، جيميني (صفر استدعاءات جديدة)، BUILD 29 (لا انقضاء وقتي للصفقات المفتوحة)، انقضاء الـPENDING بعد 24س، المسارات الخمسة.

---

## 3) الملفات

| الملف | الحالة | ما فيه |
|---|---|---|
| `services/pre_arm_ote.py` | **جديد** | كل المنطق: `detect_impulse_leg` · `project_ote` · البوابات · `try_pre_arm` (لا يرمي استثناءات أبداً) · بناء القرار |
| `tests/test_build30_prearm_ote.py` | **جديد** | 19 تثبيتاً: هندسة الرجل/الـOTE، الرفض (قديم/مسطح/صغير)، البوابات (صفقة موجودة/أخبار/اتجاه محايد/مستهلك/مفتاح الإيقاف/مبيدات لا ترمي)، الإلغاء باختراق المنطقة، البقاء، البصمة في القاعدة، مسار مستقل، قسم القياس، شكل SMC الحقيقي المضمّن |
| `services/execution_paths.py` | معدّل | تسمية `PATH_PREARM_OTE` في قاموس المسارات (تقرير أسبوعي + أداء مسارات + بطاقة إشارة) |
| `scripts/run_analysis.py` | معدّل | نقطة حقن واحدة بعد سطر BOOK وقبل المسار 2: استدعاء مُحمى — **أي** فشل = لا تجهيز والدورة تسير كما هي اليوم بالضبط |
| `agents/open_trades_manager.py` | معدّل | قاعدة `OTE_ZONE_CONSUMED` في تقييم المعلق (قبل انقضاء 24س) |
| `services/database.py` | معدّل | حفظ `armed_at` في صف الصفقة |
| `services/execution_metrics.py` | معدّل | قسم pre-arm vs reactive في Execution Quality |
| `config_snippets/pre_arm_ote.json` | **جديد** | كتلة الضبط (تُدمج في config.json — إضافة فقط) |

**ملفات BUILD 27/28/29** في الحزمة بنسختها الحالية (ذاتية الاكتمال، تُطبَّق من أي نقطة سابقة).

---

## 4) الضبط (`config.json`) — **إضافة فقط، لا استبدال**

أضف كتلة جديدة في أعلى مستوى config.json (النسخة الحالية في `payload/config_snippets/pre_arm_ote.json`):

```json
"pre_arm_ote": {
  "enabled": true,
  "timeframe": "15m",
  "entry_pct": 0.70,
  "zone_pct_low": 0.62,
  "zone_pct_high": 0.79,
  "min_displacement_atr": 1.2,
  "leg_peak_recent_candles": 3,
  "leg_lookback_candles": 15,
  "stop_buffer_atr": 1.0,
  "min_rr_ratio": 1.5,
  "max_pending_distance_points": 0,
  "require_smc_trend": true
}
```

دمج سريع بـ PowerShell (يضيف الكتلة ولا يمس أي شيء آخر):

```powershell
# بعد الخطوة 2 تكون الكتلة مستخرجة في جذر المستودع: C:\Nabil-gold\config_snippets\pre_arm_ote.json
$c = Get-Content C:\Nabil-gold\config.json -Raw | ConvertFrom-Json
$block = Get-Content C:\Nabil-gold\config_snippets\pre_arm_ote.json -Raw | ConvertFrom-Json
$c | Add-Member -Name pre_arm_ote -Value $block -Force
$c | ConvertTo-Json -Depth 20 | Set-Content C:\Nabil-gold\config.json -Encoding UTF8
```

(إن لم يجد الملف: `Expand-Archive C:\Nabil-gold\Nabil-gold-BUILD30-PREARM-OTE-20260823.zip -DestinationPath "$env:TEMP\b30" -Force` ثم خذ `…\payload\config_snippets\pre_arm_ote.json` من هناك.)

**مفتاح الإيقاف (kill switch):** `"enabled": false` → الوضع يطفو بالكامل والعودة السلوك اليوم. **كل القيم أعلاه افتراضيات مضمّنة في الكود** — عدم وجود الكتلة أصلاً = يعمل بالافتراضيات.

---

## 5) شكل التنفيذ

```powershell
# 1) نقل الحزمة
Move-Item "$env:USERPROFILE\Downloads\Nabil-gold-BUILD30-PREARM-OTE-20260823.zip" "C:\Nabil-gold" -Force

# 2) التطبيق (نسخ احتياطي + استخراج + py_compile + الاختبار الكامل + تراجع تلقائي + إعادة SS_DemoLoop وSS_TickManager)
powershell -ExecutionPolicy Bypass -File C:\Nabil-gold\deploy\APPLY_PATCH.ps1 -ZipPath "C:\Nabil-gold\Nabil-gold-BUILD30-PREARM-OTE-20260823.zip" -Force

# 3) دمج كتلة config (مرة واحدة فقط — الخطوة أعلاه)

# 4) التحقق النهائي
cd C:\Nabil-gold
python -m pytest tests -q                      # متوقع: 2507 passed / 3 skipped / 0 failed
python -m pytest tests\test_build30_prearm_ote.py -q   # 19/19
Select-String "BUILD 30" scripts\run_analysis.py | Select-Object -First 2
python -c "import json; c=json.load(open('config.json')); print(c['pre_arm_ote']['enabled'])"   # True
```

---

## 6) تحذيرات وقواعد (محدثة)

1. كل قواعد الجولات السابقة سارية (لا خفض عتبة النفط 55، الاختبارات شرط التسليم، منحنى النفط دورياً، EIA أسبوعية، مخزن جيميني 30د + عرض فقط + جلب عند الدخول/الساعة، لا انقضاء وقتي للصفقات المفتوحة).
2. **جديد — دائم**: بعض التجهيزات المسبقة **تُبطل** قبل التنفيذ — هذا الثمن المعلن للتحرك المبكر (النفذ يستهلك ولا يُعاد على نفس المستوى). راقب سطري القياس أسبوعياً: `Pre-arm OTE` مقابل `reactive`.
3. **جديد**: أي طلب `PENDING` عليه `armed_at: pre_arm_ote` يظهر في بطاقته بترويسة `🛣️ PRE-ARM OTE` — إن رأيت طلباً بهذه الترويسة **لم يُبطل ولم ينفذ** وسعره عبر المنطقة، فسيُلغى تلقائياً بوصفه مستهلكاً (لا مطاردة).
4. **جديد**: إن أراد الإيقاف الفوري: `pre_arm_ote.enabled = false` في config (يعاد تحميله تلقائياً في الدورة التالية) — لا حاجة لإلغاء يدوي للطلبات المعلقة (تُسقطها قواعدها: 24س/الهيكل/الاختراق).
5. **القياس هو الحكم**: إذا لم يُظهر المقاس أسبوعاً بأسبوع أن التجهيز يبني (دخول أفضل/نتيجة أسوأ)، التوسيع لمسارات التصويت (B/C) لا يُفتح إلا بأرقام — كما اتفقنا.

---

**نهاية التسليم — BUILD 30: 2507 خضراء في بيئة العمل + التجهيز المسبق حي على الوسيط (LIMIT حقيقي) + بصمة armed_at + قسم قياس مستقل + حزمة 27 ملفاً محققة البصمات.**
