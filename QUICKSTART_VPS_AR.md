# جاهز للنسخ — دليل التشغيل السريع على VPS ويندوز

هذه الحزمة = كامل المشروع بحالة فرع demo/mt5 (كل مراحل 08-06/08).
انسخ المجلد كله إلى C:\Nabil-gold ثم:

1. ثبّت MetaTrader 5 وسجّل دخول حساب الديمو مرة واحدة (يقلع مع الويندوز).
2. عبّئ ‎.env من ‎.env.example (ديمو فقط؛ الحقول MT5_* وشات الديمو).
3. نفّذ deploy\trades_demo.sql مرة واحدة في Supabase SQL Editor.
4. انقر START_HERE.bat (كمسؤول): يثبّت بايثون والحزم وMetaTrader5،
   يجدول SS_DemoLoop (كل 5 د) وSS_DemoWatchdog (كل دقيقة)، ويشغّل اختبار الدخان.
5. ظهور «SMOKE OK» = جاهز. البطاقات تصل لشات الديمو ببادئة 🧪 DEMO.

بدون VPS الآن؟ ارفع نفس هذه الحزمة لفرع demo/mt5 على GitHub كما هي،
وظل المرحلة 3 يعمل عبر demo_cycle.yml (التقرير اليومي يقارن الدفترين).

لا حساب حقيقي في هذه المرحلة. main لا يُمَس.
