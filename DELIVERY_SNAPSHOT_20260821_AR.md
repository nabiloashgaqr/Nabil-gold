# ملف تسليم — Nabil-gold (نظام التداول الآلي Multi-Agent)
## نسخة: BUILD 26 — تاريخ التجميع: 2026-08-21

---

## 1) الحزمة والملفات

| الملف | الوصف |
|---|---|
| `Nabil-gold-SNAPSHOT-20260821.zip` | **سناب شوت كامل للنسخة النهائية** (605 ملف كود/إعداد/أدوات — 4.3MB) |
| `Nabil-gold-BUILD26-PER-SYMBOL-CONFIRMERS-20260821.zip` | آخر رقعة منطق (تُطبَّق فوق الـVPS) |

**اختبارات النسخة:** `2451 passed / 3 skipped / 0 failed` (بتشغيل محاكاة بيئة الـVPS: TRADES_TABLE=trades_demo).

**أمان السناب شوت:** مُستبعد منه عمداً: `storage/` (بيانات الـVPS الحية — تُحمى من الكتابة فوقها)، `.pytest_cache/`، `__pycache__/`، `.git/`. عند الاستخراج فوق VPS قائم **لن تُمَس storage إطلاقاً**.

---

## 2) بصمات الملفات الحرجة (sha256 المختصر — لإثبات النسخة على أي جهاز)

```
16b27ad23bff2ba5  config.json
11b3435faaf5011a  services/thesis_consensus.py
c84b8238225994fc  services/llm_review.py
49e1a17d45d8df79  services/session_planner.py
3bd00eaf71463702  services/dashboard.py
6dd4ee50ae1e042b  services/agent_measurement.py
d9abd82b74b98985  scripts/run_analysis.py
00a094e11768df2b  scripts/run_tick_manager.py
0098058a3d5f135d  scripts/run_daily_report.py
2cf4f5891d59b165  scripts/run_trade_updates.py
edeefe8fca29d322  scripts/fill_oil_macro.py
db2822a42fd6d7c0  scripts/oil_path_audit.py
3d3e4f7b852cc5c9  scripts/gemini_model_probe.py
df9936db6e3e533a  scripts/build_oil_auction_calibration.py
10c18fcfe93e70e1  scripts/backfill_close_stamps.py
c5c1761db4e3b628  agents/auction_flow_agent.py
46c62ea6fbf524bc  agents/oil_macro_agent.py
7eb4c477ada775fd  agents/risk_management_agent.py
e1e5d9e4d8082851  utils/instruments.py
c3680469396cdbe7  utils/trading_rules.py
d30cf4638cf2e6c7  services/telegram_bot.py
351fbe4631a4bf85  services/weekly_report.py
a184c8aeacb98ff0  services/dashboard_local_api.py
137f866050fab013  dashboard/app.js
aba750a1b0cfd2cd  dashboard/index.html
3050d29f99198a81  subscription_bot/database.py
```

---

## 3) ملخص الأبنية (1–26)

### الأبنية التأسيسية (1–15 — قبل 20/8)
المرحلتان 1+2، إصلاحات الوكلاء (Stochastic/Ichimoku/هارمونيك/H&S/روزنامة/MAE-MFE)، كاش جيميني بإبطال تقلبي 25%، فحص MT5، psutil، مفتاح قتل المسار 1، تصفير السقف اليومي، أوامر المشغل (مشاهدة/نفط/سقف/حدود/أرضية 60/ARMED MAP/تليجرام)، crack تلقائي، مفاجآت real، حارس المعلقات، **سقف لكل زوج (ذهب 2 + نفط 2، حزام إجمالي 4)**.

### بولش تليجرام واللوحة (16–17)
- رسالة حالة **واحدة/ساعة** (حارس dedup + حذف SS_MarketStatus) بشكل موحد: توقيت، أسعار، قرار، كتاب الوكلاء الستة، بوابات.
- بطاقات الصفقات الموحدة برأس المسار (تفعيل/تعادل/تريلنج/إغلاقات/إلغاءات) — حتى من مدير التكات.
- **ديمو فقط** في كل التقارير واللوحة (فلتر صفوف صارم `mt5_demo`).
- التقارير: يومي ولوحة **23:50** بتوقيت المشغل، أسبوعي **الجمعة 23:55**، إصلاح تاريخ اليومي (قبل 4 فجراً يرجع ليوم المنتهي).
- اللوحة: **إنجليزية افتراضياً**، تنظيف الخلط العربي، حارس اختبارات ضد التسرب.
- بوت المشتركين: **محلي كل 4 ساعات** بلا Supabase (fallback JSON).

### سلسلة النفط كاملة (18–20.2)
- **تغذية تكات حية** لكل رمز مفعّل (اشتراك Market Watch + سقوط وقت) — أنهت موت المسارين 4/5.
- **سقوف أهداف النفط** 150/350 نقطة (قانون الأهداف + فلتر الإرسال).
- **قوانين النفط لكل صف حي** (ستوب 60/21/120، تعادل +70، تريلنج 70/25، قيمة نقطة 0.01$) — قضت على كابوس ستوب 2.70$.
- **معايرة auction حقيقية للنفط**: 2.27 مليون تكة حقيقية → منحنى WTI.s بمتانة span=11.46pp (بوابة صحّة ترفض المنحنيات الضعيفة).
- **EIA يومي تلقائي** (أسماء API v2 المجردة + حدود تاريخ + محلل مزدوج + مسار seriesid) — ملف حقيقي مقروء.

### جيميني (21–23.1)
- حالة دائمة عبر العمليات (ملف) + **تجاوز النماذج الميتة** (حصة لكل نموذج، انتهاء بتوقيت المحيط الهادئ، حلقة لا تستهلك المحاولات).
- **نماذج مثبتة لمفتاح المشغل**: أساسي `gemini-3.5-flash-lite` (500/يوم) + `3.1-flash-lite` (500/يوم) + ذيول (3.6/3.5-flash، flash-latest) — **1000 طلب يومياً، لا مرفوض واحد في القائمة**.

### سلامة ما بعد الخروج والأرقام (22–22.2)
- **ختم closed_at بوقت صفقة الوسيط الحقيقي** (مع تثبيت الساعة المنزاحة +3) — عاد حارس ما بعد TP2 (2.5 ساعة/250 نقطة) يعمل.
- إصلاح تاريخي للصفوف المغلقة بلا ختم.
- **نقاط الإغلاق بقيمة نقطة كل رمز** (وداعاً −12 بدل −120) + الدولار الحقيقي في البطاقات + سعر الصف في كل بطاقة.

### الآراء والعزل النهائي (24–26)
- رسالة الحالة: **ماكرو ذهب + ماكرو نفط** (محليان كل ساعة) + **سطر جيميني لكل رمز** (كل ساعتين، مخزن أولاً، مفاتيح بالرمز).
- **عزل نهائي لكل زوج**: الماكرو (قرار+عرض)، جيميني (مدخلات+شخصية+مخزن)، الـAuction (متجر+معايرة+تغذية) — بلا خلط، والذهب سلوكه لم يتغير حرفياً.

---

## 4) الحالة الحية على الـVPS (كما أُكدت آخر مرة)

| المكوّن | الحالة |
|---|---|
| المهام | SS_DemoLoop ✓ · SS_DemoAnalysis ✓ · SS_TickManager ✓ · SS_DashboardAPI ✓ · SS_SubscriptionBot (كل 4h) ✓ · SS_EIAFill (يومي 15:10 UTC) ✓ |
| MT5 | حساب ديمو 1200315085 متصل، تنفيذ ذهب + نفط LIVE |
| جيميني | مفتاح حي، 3.5-flash-lite أساسي، تنقل بين نماذج مثبتة فقط |
| النفط | تغذية حية + منحنى معاير 11.46pp + سقوف أهداف + قوانين نفط لكل صف |
| EIA | ملف حقيقي (أسبوع 2026-08-14) + مهمة يومية |
| التقارير | يومي/لوحة 23:50، أسبوعي جمعة 23:55 — ديمو فقط |
| الاختبارات | آخر تطبيق: 2432 passed / 9 skipped (قبل 26) |

---

## 5) كيفية الاستخدام

### أ) تثبيت جديد من الصفر (خادم آخر):
1. استخرج `Nabil-gold-SNAPSHOT-20260821.zip` إلى `C:\Nabil-gold` (المجلد الداخلي Nabil-gold-CLEAN هو الجذر).
2. انسخ `.env.template` إلى `.env` واملأ المفاتيح (TELEGRAM، GEMINI، EIA، MT5).
3. `powershell -ExecutionPolicy Bypass -File deploy\vps_setup.ps1` (ينشئ المهام).
4. `python -m pytest tests -q` (يتوقع 2451 خضراء).
5. شغّل أدوات الإعداد الاختيارية: `deploy\SETUP_EIA_DAILY.ps1` (مهمة EIA)، `deploy\UPDATE_SCHEDULES_20260819.ps1` (مواعيد 23:50/جمعة).

### ب) تحديث VPS قائم:
طبّق رقعة البناء 26 فقط — لا تستخرج السنابشوت فوق VPS قائم (storage محمية بالتصميم لكن الأرشفة أنظف):
```powershell
powershell -ExecutionPolicy Bypass -File C:\Nabil-gold\deploy\APPLY_PATCH.ps1 -ZipPath "C:\Nabil-gold\Nabil-gold-BUILD26-PER-SYMBOL-CONFIRMERS-20260821.zip" -Force
```

---

## 6) فهرس الأدوات التشغيلية (كلها قرائية ما لم يُنص على غيرها)

| الأداة | الغرض |
|---|---|
| `deploy\APPLY_PATCH.ps1` | تطبيق الرقع (نسخ احتياطي + اختبارات + تراجع تلقائي) |
| `CHECK_EVERYTHING.ps1` | الفحص الشامل (SYSTEM/MODES/OIL/MACRO/MAPS) |
| `scripts\oil_path_audit.py` | تدقيق سلسلة النفط (9 أقسام) |
| `scripts\gemini_model_probe.py` | فحص نماذج مفتاح جيميني (OK/404/429/AUTH) |
| `scripts\oil_macro_check.py` | فحص الماكرو النفطي الحي |
| `scripts\build_oil_auction_calibration.py` | إعادة بناء منحنى النفط (بوابة صحّة تلقائية) |
| `scripts\backfill_close_stamps.py` | ختم الصفوف المغلقة (من سجل الوسيط، بتصحيح الساعة) |
| `scripts\fill_oil_macro.py` | تعبئة ملف EIA (أسماء v2 + seriesid) |
| `deploy\OIL_PATH_AUDIT.ps1` / `FINAL_VERIFY.ps1` | أغلفة فحص |
| `deploy\UPDATE_SCHEDULES_20260819.ps1` | المواعيد (23:50/جمعة/بوت 4h) |
| `deploy\SETUP_EIA_DAILY.ps1` | مهمة EIA اليومية |

---

## 7) تحذيرات وقواعد التشغيل

1. **لا تلمس الصفقة الحية** `TRADE_20260821_091635` (4590) — قرار المشغل: تُدار بقوانينها حتى نهايتها.
2. **لا تخفض عتبة تأكيد النفط** (55) — قرار مشغل سابق.
3. **سقف الأوامر مفتوح (0)** وحدود الخسائر النقطية/الدولارية محذوفة — الحماية = halt_after_losses=3 فقط.
4. **الاختبارات شرط التسليم**: أي تغيير منطق → `python -m pytest tests -q` خضراء قبل الحزمة.
5. **إعادة بناء منحنى النفط كل 3–4 أسابيع** (أو بعد تحول نظام سوقي) — البوابة تنحّي الضعيف تلقائياً.
6. **جيميني**: حصص النماذج تنتهي منتصف لوس أنجلوس — النظام يتنقل تلقائياً.
7. **بيانات EIA أسبوعية** (الأربعاء) — المهمة اليومية تشفي أي غياب.

---

## 8) المتبقي الاختياري (بيد المشغل دائماً)
- ملف `storage\oil\opec_context.json` (يدوي بعد كل اجتماع OPEC+ — قالب جاهز).
- أي إضافة لعتبات جديدة = رقعة منطق باختبارات.

**نهاية التسليم — النسخة مغلقة ومختبرة بالكامل.**
