# خطة النقل: خادم خاص + MetaTrader 5 — مع بقاء Supabase والتيليجرام واللوحة

تاريخ الخطة: 2026-08-06 · حالة: **للفهم والموافقة — لم يُلمس أي كود بعد**

## ١) ماذا يبقى بلا أي تغيير (اطمئن)

| المكوّن | لماذا يبقى |
|---|---|
| **Supabase (كل الجداول والتاريخ)** | نفس `services/database.py` يكتب بنفس الأعمدة — الصفقات القديمة والجديدة في سجل واحد متصل |
| **قناة تيليجرام** | نفس `services/telegram_bot.py` ونفس التوكن — البطاقات لا تتغير |
| **لوحة القناة (الموقع)** | `api/dashboard.js` يقرأ من Supabase مباشرة — لا يعلم أصلاً أين يعمل المحرك |
| **كل المنطق** | الوكلاء، المخطط، التريلنج، الاختبارات (1631) — كما هي |

**الخلاصة: نغيّر "العين" (مصدر السعر) و"المكان" (المضيف)، ولا نغيّر "الذاكرة" (قاعدة البيانات) ولا "الفم" (تيليجرام).**

## ٢) ماذا يتغير

1. **المضيف**: GitHub Actions ← خادمك الخاص (VPS).
2. **مصدر السعر**: TwelveData ← MetaTrader 5 (شموع لحظية من الترمينال).
3. **المجدول**: cron-job.org ← مجدول محلي على السيرفر.
4. **الأسرار**: GitHub Secrets ← ملف `.env` على السيرفر (نفس القيم).

## ٣) حقيقة تقنية تحدد الخيار (مفترق الطرق)

حزمة `MetaTrader5` الرسمية لبايثون تعمل على **Windows فقط**. لذلك:

- **خيار أ (موصى به)**: VPS بنظام Windows — ترمينال MT5 + بايثون على نفس الجهاز. أبسط وأمتن.
- **خيار ب**: Linux + جسر أسعار (EA داخل MT5 عند وسيط آخر) — تعقيد إضافي بلا حاجة الآن.

## ٤) الخطوات بالترتيب (للفهم قبل التنفيذ)

### مرحلة ٠ — تجهيز (النظام الحالي لا يُمَس)
1. استئجار Windows VPS ‏(2-4 أنوية / 8GB رام كافية).
2. تثبيت MT5 وتسجيل الدخول بحساب **تجريبي**.
3. تثبيت Python ‏3.11+ ثم `pip install -r requirements.txt` و `pip install MetaTrader5`.
4. `git clone` للمستودع + ملف `.env` بنفس أسرار GitHub.

### مرحلة ١ — الكوبري (الكود الجديد الوحيد)
5. ملف جديد `services/mt5_feed.py`: يقرأ الشموع بـ `copy_rates_from` ويعيدها **بنفس شكل** ما ترجعه `get_gold_data` اليوم (نقطة الالتصاق الوحيدة في `services/market_data.py`).
6. **تحويل زمن شمعة MT5 (توقيت البروكر) إلى UTC** — حرج جداً: منطق `trailing_stop_source_time` وإعادة تشغيل الشموع يعتمدان على الطوابع الزمنية.
7. مفتاح في `config.json`: `data_source: "twelvedata" | "mt5"` — الافتراضي يبقى TwelveData حتى يثبت MT5 نفسه.

### مرحلة ٢ — الظل (أسبوع إلى أسبوعان)
8. السيرفر يحلّل **بدون إرسال تيليجرام وبدون كتابة صفقات** — سجل محلي فقط.
9. قياس التطابق: نفس الشموع؟ نفس القرارات؟ كم دورة اختلفت؟ (ثقافتنا: بالأرقام لا بالانطباع).

### مرحلة ٣ — القلب التدريجي
10. نقل `run_trade_updates` (إدارة الصفقات) أولاً ومراقبة يوم كامل.
11. ثم نقل `run_analysis` (الإشارات).
12. إيقاف وظائف cron-job.org واحدة واحدة؛ يبقى GitHub Actions احتياطاً يدوياً.

### مرحلة ٤ — تنفيذ حي على MT5 (قرار منفصل)
13. `order_send` حقيقية — **لن تُلمس قبل موافقتك الصريحة**؛ الورقي يستمر حتى تقرر.

## ٥) الملفات المطلوبة للنقل

- **كامل المستودع كما هو**: `scripts/` `services/` `agents/` `config.json` `tests/`.
- **جديد فقط**:
  - `services/mt5_feed.py` (كوبري الأسعار + تحويل UTC)
  - `scripts/run_scheduler.py` أو مهام Windows (المجدول)
  - `.env.example` + سكربت watchdog يرسل نبضة تيليجرام إن توقف المحرك
- **بلا تغيير**: `services/database.py` · `services/telegram_bot.py` · `api/dashboard.js` · `supabase_schema_unified.sql`.

## ٦) هل يبقى كل شيء مسجلاً؟ — نعم

- التاريخ الحالي لا يُلمس (نفس الجداول).
- الصفقات الجديدة تُكتب بنفس الكود → اللوحة تعرض القديم والجديد في خط واحد.
- أثناء مرحلة الظل لا تُكتب صفقات مكررة (الكتابة معطلة عمداً هناك).
- عند القلب: مصدر واحد فقط يكتب في كل لحظة (إما السيرفر أو Actions، لا الاثنان) لمنع الازدواج.

## ٧) مخاطر محسوبة مسبقاً

| الخطر | التحصين |
|---|---|
| توقيت بروكر MT5 ≠ UTC | تحويل صريح واختبار بفروق الطوابع |
| اسم الرمز XAUUSD مقابل XAU/USD | خريطة رموز في `mt5_feed.py` |
| انقطاع الترمينال/الجلسة | watchdog + إعادة اتصال تلقائية + نبضة تيليجرام |
| ازدواج كتابة أثناء القلب | مصدر واحد فقط يكتب (بند ١٢) |
| عطلة/ويكند | سلوك "السوق مغلق" نفسه الموجود اليوم |

## ٨) الكلفة التقريبية

Windows VPS ‏≈ 10-25$/شهر مقابل استقرار كامل وتحكم لحظي — وبديل عن اعتماد الليلة على طوابير GitHub.

---
**بانتظار موافقتك لنبدأ مرحلة ١ (بناء الكوبري) — وحتى ذلك الحين لا يتغير شيء في النظام الحي.**
